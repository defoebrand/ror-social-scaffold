<section class="user-section">
  <h2><%= "@#{@user.name}" %></h2>

  <%= friend_response_show(@user) %>

  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
